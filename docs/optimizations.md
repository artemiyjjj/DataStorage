# Storage workload and recource consumption optimization

Обоснование идей по оптимизации хранилища и описание реализаций этих идей в проекте.

## Agenda

При построении документного хранилища, существует , учёт которых может привести к оптимизации частных случаев запросов к хранилищу в разы.

Реализация этого хранилища не эталонна и имеет свои особенности, принятые на уровне проектирования. Примером таких особенностей выступает пространственная локальность для данных одного типа, т.е. хранение данных одного типа в одном участке хранилища. Это позволяет использовать особенность `Запросы-1`.

### Блоки

1. Разделение хранилища на блоки

    В целях упрощения индексации элементов, 


1. Использование блоков для данных одного типа

    



### Клетки

### Хранилище

### Запросы

1. Выборка 

    Благодаря особенности [Блоки-2](#блоки), при обработке запросов к хранилищу, содержащих условия с проверкой типов данных узлов, не требуется обходить всё дерево с помощью поиска в глубину, начиная с корневого элемента всего дерева или любого поддерева - достаточно обратиться к списку структур, содержащих блоки с данными соответствующего типа и просматривать только их.

